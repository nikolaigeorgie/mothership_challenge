import React, { PureComponent } from 'react';
import MenuScreenView from './Views';

class MenuScreen extends PureComponent {
  render() {
    return <MenuScreenView />;
  }
}

export default MenuScreen;
